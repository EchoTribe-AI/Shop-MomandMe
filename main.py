# ARCHIVED: This module is no longer wired into the app as of the
# Shop-MomandMe strip-down PR (2026-05-17). It was a Replit boilerplate
# entrypoint from the original "repl-nix-workspace" template and never
# carried real logic. Retained for reference per the strip-down brief
# ("do NOT delete main.py — preserve as archive"). If you're certain it's
# unused everywhere, remove in a follow-up commit with rationale.


def main():
    print("Hello from repl-nix-workspace!")


if __name__ == "__main__":
    main()
